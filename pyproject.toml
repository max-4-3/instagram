[project]
name = "app"
version = "0.1.0"
description = "Downloader (new) for instagram"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.12.15",
    "py-dotenv>=0.1",
    "pydantic[email]>=2.11.7",
    "rich>=14.1.0",
]
